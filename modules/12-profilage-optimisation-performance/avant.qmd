---
title: "12A - Profilage, optimisation et performance - Avant la classe"
format:
  html:
    embed-resources: true
---

## Objectif de préparation

Comprendre la méthodologie scientifique de mesure de performance en R, installer les packages nécessaires (`bench` et `profvis`) et exécuter un benchmark simple.

Cette page représente environ 35 minutes de la [préparation unique de 50 minutes](../12-synthese-performance-parallele/index.qmd): 25 minutes de lecture et d'exécution, puis 10 minutes pour le seul mini-test de la semaine.

## À lire

1.  **Advanced R (Hadley Wickham)** :
    *   [Chapter 23: Measuring performance](https://adv-r.hadley.nz/perf-measure.html) : Comprendre comment profiler avec `profvis` et faire des microbenchmarks.
    *   [Chapter 24: Improving performance](https://adv-r.hadley.nz/perf-improve.html) : Lire l'introduction et la section **Organise your code** (identifier les allocations mémoires).

---

## Exemples R à exécuter

Veuillez ouvrir votre session Positron ou RStudio et exécuter les exemples de code suivants.

### Exemple 1 : Micro-benchmarking avec `bench::mark()`

Le package `bench` mesure le temps d'exécution (avec une précision de l'ordre de la nanoseconde) ainsi que l'allocation mémoire de chaque expression, tout en comptabilisant le nombre de fois où le ramasse-miettes (garbage collector - `n_gc`) a dû intervenir.

```r
library(bench)

# Simulation d'un vecteur d'entiers avec valeurs manquantes
set.seed(42)
x <- sample(c(1:10, NA), size = 100000, replace = TRUE)

# Comparons trois méthodes pour calculer la proportion de NA
resultats_bench <- mark(
  comparaison_directe = mean(is.na(x) == TRUE),
  moyenne_logique     = mean(is.na(x)),
  somme_div_longueur  = sum(is.na(x)) / length(x),
  check = TRUE # Valide que toutes les expressions retournent le même résultat
)

print(resultats_bench)
```

**À observer** :
*   Quelle colonne indique le temps médian ?
*   Quelle méthode alloue le moins de mémoire (`mem_alloc`) ?
*   Pourquoi `mean(is.na(x) == TRUE)` est-il un peu plus lent que `mean(is.na(x))` ?

### Exemple 2 : Profiler du code avec `profvis`

Le profilage consiste à arrêter périodiquement R (toutes les millisecondes) pour enregistrer la pile d'appels active. Cela permet de dresser une carte visuelle du temps passé dans chaque fonction.

```r
library(profvis)

# Lancer le visualiseur interactif de profvis
profvis({
  # Simulation d'un calcul itératif lourd avec croissance d'objet en mémoire
  somme_de_vecteur_croissant <- function(somme_visee = 50000) {
    somme <- 0
    resultats <- integer(length = 0) # Débuter avec un vecteur vide
    while (somme < somme_visee) {
      tirage <- sample(1:6, size = 1)
      somme <- somme + tirage
      # ALERTE PERFORMANCE : Grossissement dynamique du vecteur à chaque itération
      resultats <- c(resultats, tirage) 
    }
    resultats
  }
  
  simulations <- somme_de_vecteur_croissant(100000)
})
```

**À observer** : Dans l'onglet interactif qui s'ouvre, repérez la ligne qui affiche une barre jaune importante pour `Alloc` (allocation mémoire) et `Dealloc` (désallocation / garbage collection).

---

## Mini-test de préparation

Ce mini-test fait partie de la composante « Mini-tests de préparation » décrite dans la page [Évaluations](../../evaluations.qmd) et suit le [gabarit commun](../../gabarits/gabarit-mini-test-preparation.qmd).

- Durée cible: 5 à 10 minutes, comprise dans la préparation unique de la semaine 12.
- Questions: répondre aux trois questions ci-dessous dans l'ordre.
- Format de remise: trois réponses numérotées dans le canal officiel indiqué par l'équipe enseignante.
- Échéance: remettre avant le début du laboratoire.
- Règle IA: l'IA est permise pour clarifier une notion. Les réponses doivent être comprises et vérifiées. Une clarification mineure n'exige pas d'entrée détaillée; si l'aide influence du code, une décision ou une trace conservée, la consigner dans `docs/ai-log.md`.
- Trace: conserver le résultat dans le canal de remise et apporter en classe la difficulté relevée.
- Complétion minimale: les trois réponses sont présentes, l'exemple `bench::mark()` a été exécuté et l'observation de la question 3 est rapportée sans cible de vitesse.

### Question 1 : Conclusion défendable avant la mesure
Les instructions suivantes visent la même proportion de données manquantes dans un vecteur d'entiers `x`. Quelle conclusion est méthodologiquement défendable avant d'avoir exécuté un benchmark sur la machine utilisée?

```r
opt_a <- mean(is.na(x) == TRUE)
opt_b <- mean(is.na(x))
opt_c <- sum(is.na(x)) / length(x)
opt_d <- tail(proportions(table(x, useNA = "always")), n = 1)
```

*   **Option A:** `opt_a` sera toujours la plus rapide.
*   **Option B:** `opt_b` sera toujours la plus rapide.
*   **Option C:** `opt_c` sera toujours la plus rapide.
*   **Option D:** aucune vitesse absolue ne peut être affirmée sans mesure; il faut aussi vérifier que les résultats comparés sont équivalents.

### Question 2 : Choix de l'indicateur de centralité
Pourquoi est-il fortement recommandé de s'appuyer sur la **médiane** des temps d'exécution plutôt que sur la **moyenne** lors de la comparaison de deux algorithmes R ?

*   **Option A :** La moyenne est biaisée par le temps CPU des autres cœurs de la machine.
*   **Option B :** La moyenne est sensible aux valeurs extrêmes causées par le déclenchement ponctuel du ramasse-miettes (Garbage Collector).
*   **Option C :** R calcule la médiane plus rapidement que la moyenne.
*   **Option D :** La médiane ignore les lancers de dés aléatoires.

### Question 3 : Observation personnelle et vérifiable

Après avoir exécuté l'exemple `bench::mark()`, indiquez l'ordre des trois expressions selon leur temps médian sur votre machine, puis notez un facteur qui limite la comparaison, par exemple la charge de fond, le nombre d'itérations ou la version de R. Signalez toute erreur d'installation ou d'exécution à reprendre en classe.
