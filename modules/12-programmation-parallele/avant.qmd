---
title: "12B - Programmation asynchrone et parallèle - Orientation facultative"
format:
  html:
    embed-resources: true
---

## Objectif de préparation

Décider si une tâche déjà profilée mérite un essai parallèle et connaître les garde-fous avant d'ouvrir des processus locaux.

Cette orientation représente environ 10 minutes de la [préparation unique de 50 minutes](../12-synthese-performance-parallele/index.qmd). L'installation et l'exécution des exemples peuvent se faire en classe seulement si cette voie est choisie.

Il n'y a aucun mini-test propre au bloc 12B. Le seul mini-test de la semaine se trouve dans la page [12A - Avant la classe](../12-profilage-optimisation-performance/avant.qmd).

## Décider avant de paralléliser

Choisissez cette voie seulement si le profilage suggère une tâche répétée, indépendante, sans écriture concurrente et suffisamment coûteuse relativement au transfert des données. Sinon, poursuivez l'optimisation séquentielle.

## À lire

1.  **Vignette officielle du package `future`** :
    *   [A Future for R: A System for Friendly Parallel Processing](https://future.futureverse.org/articles/future-1-overview.html) : Comprendre la philosophie des futurs (unification de la syntaxe parallèle en R).
2.  **Vignette officielle du package `furrr`** :
    *   [Introduction to furrr](https://furrr.futureverse.org/) : Comprendre comment combiner la puissance de `purrr` (itération) et de `future` (calcul parallèle).

---

## Exemples R à exécuter

Veuillez ouvrir votre session Positron ou RStudio et exécuter les exemples suivants.

### Exemple 1 : Créer et résoudre un futur (asynchrone)

Un *futur* est une abstraction pour un calcul qui sera résolu plus tard, éventuellement sur un processus en arrière-plan.

```r
library(future)

# 1. Calculer un nombre prudent de workers
coeurs_disponibles <- as.integer(availableCores())
workers <- max(1L, min(2L, coeurs_disponibles - 1L))

if (workers >= 2L) {
  plan(multisession, workers = workers)
} else {
  message("Un seul coeur est disponible: repli séquentiel.")
  plan(sequential)
}

mode_execution <- if (workers >= 2L) "multisession" else "séquentiel"
cat("Lancement du calcul avec le plan", mode_execution, "...\n")
temps_depart <- Sys.time()

# 2. Création d'un futur : le code s'exécute en tâche de fond
f <- future({
  Sys.sleep(2) # Simule un calcul de 2 secondes
  42
})

cat("Temps consacré à la création ou à l'exécution du futur :",
    round(Sys.time() - temps_depart, 2), "secondes.\n")

# 3. Récupérer le résultat (bloquant si le calcul n'est pas terminé)
valeur_finale <- value(f)

cat("Résultat récupéré :", valeur_finale, ". Temps total écoulé :", 
    round(Sys.time() - temps_depart, 2), "secondes.\n")

# 4. Toujours revenir au mode séquentiel
plan(sequential)
```

### Exemple 2 : Itération parallèle avec `furrr`

`furrr` fournit des équivalents parallèles de `purrr::map()`. Le préfixe `future_` permet de distribuer les itérations sur les processus ouverts.

```r
library(purrr)
library(furrr)

# 1. Vecteur d'étapes simulant des calculs indépendants
taches <- rep(0.25, 4)

# 2. Mesure séquentielle de référence
temps_seq <- system.time({
  res_seq <- map(taches, ~ { Sys.sleep(.x); .x * 2 })
})
cat("Temps séquentiel :", temps_seq["elapsed"], "secondes.\n")

# 3. Essai parallèle prudent ou repli séquentiel
coeurs_disponibles <- as.integer(future::availableCores())
workers <- max(1L, min(2L, coeurs_disponibles - 1L))

if (workers >= 2L) {
  future::plan(future::multisession, workers = workers)
} else {
  message("Un seul coeur est disponible: future_map() utilisera le plan séquentiel.")
  future::plan(future::sequential)
}

temps_par <- system.time({
  res_par <- future_map(taches, ~ { Sys.sleep(.x); .x * 2 })
})
cat("Temps parallèle :", temps_par["elapsed"], "secondes.\n")

future::plan(future::sequential)
```

---

## Auto-vérification facultative et non remise

Ces questions servent uniquement à vérifier votre compréhension si vous choisissez la voie 12B. Elles ne constituent pas un second mini-test et aucune réponse n'est remise.

### Question 1 : Le concept de surcharge (Overhead)
Pourquoi l'exécution d'une tâche simple (ex: faire la somme de 10 nombres) est-elle généralement **beaucoup plus lente** en parallèle qu'en séquentiel ?

*   **Option A :** Les processeurs modernes ralentissent leur fréquence lorsqu'ils activent plusieurs cœurs.
*   **Option B :** Le temps requis pour démarrer les processus de travail et copier les données en mémoire vers ces processus est supérieur au temps du calcul lui-même.
*   **Option C :** R limite volontairement la vitesse des cœurs secondaires pour éviter la surchauffe.
*   **Option D :** Les futurs utilisent un algorithme de tri plus lent.

### Question 2 : Reproductibilité et nombres aléatoires
Pourquoi l'utilisation de `set.seed(123)` suivi d'une boucle parallèle classique (sans configuration spécifique) pose-t-elle un grave problème pour des simulations scientifiques ?

*   **Option A :** Les processus peuvent utiliser des flux aléatoires dupliqués ou dépendants, ce qui invalide l'indépendance attendue.
*   **Option B :** Les processus de travail n'ont pas accès à l'horloge système.
*   **Option C :** La génération parallèle modifie de façon permanente le système d'exploitation.
*   **Option D :** `set.seed` ne fonctionne pas en dehors du Global Environment.

### Question 3 : Choix du plan de calcul
Quel plan de calcul `future` devez-vous utiliser en priorité si vous travaillez sur une machine Windows et souhaitez exécuter du code en parallèle sur vos cœurs locaux ?

*   **Option A :** `plan(multicore)`
*   **Option B :** `plan(cluster)`
*   **Option C :** `plan(multisession)`
*   **Option D :** `plan(sequential)`
